<!DOCTYPE html>
<html lang="en">
<head> 
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" integrity="sha384-4LISF5TTJX/fLmGSxO53rV4miRxdg84mZsxmO8Rx5jGtp/LbrixFETvWa5a6sESd" crossorigin="anonymous">

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <style>
        body {
            font-family:  sans-serif;
            background-color: #eef7ff;
        }
        .sidebar {
            background-color: #4D869C;
            color: #eef7ff;
            height: 100vh;
            position: fixed;
            width: 280px;
            overflow-y: auto;
            border: 3px solid  #4D869C;
            padding: 15px 10px;
        }
        .sidebar .nav-link {
            margin-bottom: 15px;
            padding: 12px 12px;
            text-align: center;
        }
        .sidebar .nav-link.active {
            background-color: #eef7ff;
            color: #4D869C;
            border-radius: 12px;
        }

        .in-active{
            border: 3px solid  #eef7ff;
            color: #eef7ff !important;
            border-radius: 12px;
        }

          .main-content {
            margin-left: 250px;
            padding: 20px;
        } 
        .btn-pink {
            background-color: #e91e63;
            color: white;
        }
        .table-responsive {
            overflow-x: auto;
        }
        .search-bar {
            max-width: 300px;
            margin-bottom: 20px;
        }
        .user-list-header {
            background-color: #4D869C;
            color: #EEF7FF;
            padding: 10px;
            border-radius: 5px 5px 0 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .search-input {
            padding: 5px 10px;
            border: none;
            border-radius: 5px;
            margin-left: 10px;
        }

        .card {
            margin-bottom: 20px;
        }

        input[type="search"]{
           color: #4D869C;
           padding: 12px 12px;
           margin-top: 5px;
        }

    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <nav class="col-md-3 col-lg-2 d-md-block sidebar collapse">
                <div class="position-sticky pt-3">
                    <div class="mb-4 px-3 text-center" >
                        <i class="fas fa-user-circle"></i> Admin Dashboard
                    </div>
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link in-active" href="/admin"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link in-active" href="/admin/userList"><i class="fas fa-users"></i> Users</a>
                        </li>
                      
                        <!-- <li class="nav-item">
                            <a class="nav-link in-active" href="/admin/paymentMethods"><i class="fas fa-credit-card"></i> Payment</a>
                        </li> -->
                        <li class="nav-item">
                            <a class="nav-link in-active" href="/admin/salesReport"><i class="fas fa-chart-bar"></i> Report</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link in-active" href="/admin/categories"><i class="fas fa-folder"></i> Category</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" href="/admin/products"><i class="fas fa-folder"></i>
                                Product Management</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link in-active" href="/admin/orders"><i class="fas fa-truck"></i> Orders Management</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link in-active" href="/admin/transactionHistory"><i class="fas fa-history"></i> Transaction history</a>
                        </li>
                       
                       
                        <li class="nav-item">
                            <a class="nav-link in-active" href="/admin/couponsHistory"><i class="fas fa-ticket-alt"></i> Coupons history</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link in-active" href="/admin/notifications"><i class="fas fa-bell"></i> Notifications</a>
                        </li>
                    </ul>
                    <div class="mt-5 px-3">
                        <a href="/admin/logout"><button class="btn btn-outline-light w-100" style="padding: 12px;background-color: #EEF7FF;color: #4D869C;margin-top: -20px;">LOGOUT</button></a>
                    </div>
                </div>
            </nav>

            <!-- Main content -->
            <main class="ms-sm-auto  px-md-4 main-content  p-5" style="width: 80%;">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2" style="color:#4D869C">Dashboard / Product Management</h1>
                    <a href="/admin/addProducts">
                        <button class="btn btn-outline-dark me-2" style="border: 1px solid #4D869C;background-color: #4D869C;color: #eef7ff;">
                            Add Product
                        </button>
                    </a>
                </div>


                <div class="bg-white p-4 rounded shadow-sm mt-3 border" style="border-color: #4D869C !important;">
                    <div class="user-list-header">
                        <h2>Products List</h2>
                        <div class="search-bar">
                            <input type="search" class="search-input" id="productSearch" placeholder="Search for products.." onkeyup="searchTable()">
                        </div>
                    </div>
                    <div class="table-responsive" style="color: #4D869C;overflow: auto;">
                        <table class="table table-hover" id="productTable" style="color: #4D869C;width: 70em;">
                            <thead>
                                <tr class="text-center">
                                    <th>No.</th>
                                    <th>Product</th>
                                    <th>Date</th>
                                    <th>Variants</th>
                                    <th>Total Count</th>
                                    <th>Gender</th>
                                    <th>Price</th>
                                    <th>Brand</th>
                                    <th>Category</th>
                                    <th>Options</th>
                                </tr>
                            </thead>
                            <tbody style="color: #4D869C;"  id="productTableBody" >
                                <% productData.forEach(function(product, index) { %>
                                    <tr class="text-center">
                                        <td><%= index + 1 %></td>
                                        <td><%= product?.name %></td>
                                        <td><%= new Date(product?.updatedAt).toLocaleDateString('en-US') %></td>
                                        <td>
                                            <% product.variants.forEach(function(variant) { %>
                                                <div class="variant-info">
                                                    <img src="<%= variant.imageUrls[0] %>" alt="Image" width="50">
                                                    <span><b>Color:</b> <%= variant.color %></span>
                                                    <span><b>Size:</b> <%= variant.size %></span>
                                                    <span><b>Stock:</b> <%= variant.stock %></span>
                                                </div>
                                            <% }) %>
                                        </td>
                                        <td>
                                            <%= product.variants.reduce((total, variant) => total + variant?.stock, 0) %>
                                        </td>
                                        <td><%= product?.gender %></td>
                                        <td><%= product?.price %></td>
                                        <td><%= product?.brand %></td>
                                        <td><%= product?.category?.name %></td>
                                        <td>
                                            <a href="/admin/editProduct/<%= encodeURIComponent(product?._id) %>">
                                                <button class="btn btn-primary mx-3" style="border: 1px solid #4D869C;background-color: #4D869C;color: #eef7ff;">Edit</button>
                                            </a>
                                            <!-- <a href="/admin/softDeleteProduct/<%= encodeURIComponent(product?._id) %>"> -->
                                                <button class="btn btn-danger" style="border: 1px solid #4D869C;background-color: #4D869C;color: #eef7ff;" onClick="softDeleteProduct('<%= encodeURIComponent(product?._id) %>', this)">
                                                    <%= (product.deleted === true) ? "Unhide" : "Hide" %>
                                                </button>
                                            <!-- </a> -->
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                        <div class="pagination  d-flex justify-content-center">
                            <button class="btn " id="prevPage" onclick="prevPage()">Previous</button>
                            <button class="btn " id="nextPage" onclick="nextPage()">Next</button>
                        </div>

                    </div>
                    
                    
                </div>





                






            </main>
        </div>
    </div>

    <script>

const rowsPerPage = 2;
    let currentPage = 1;
    const tableBody = document.getElementById('productTableBody');
    const rows = tableBody.getElementsByTagName('tr');
    const totalPages = Math.ceil(rows.length / rowsPerPage);

    function displayRows() {
        for (let i = 0; i < rows.length; i++) {
            rows[i].style.display = 'none';
        }
        for (let i = (currentPage - 1) * rowsPerPage; i < currentPage * rowsPerPage && i < rows.length; i++) {
            rows[i].style.display = '';
        }
        document.getElementById('prevPage').classList.toggle('disabled', currentPage === 1);
        document.getElementById('nextPage').classList.toggle('disabled', currentPage === totalPages);
    }

    function prevPage() {
        if (currentPage > 1) {
            currentPage--;
            displayRows();
        }
    }

    function nextPage() {
        if (currentPage < totalPages) {
            currentPage++;
            displayRows();
        }
    }

    displayRows();

    function searchTable() {
        const input = document.getElementById('productSearch');
        const filter = input.value.toLowerCase();
        const rows = tableBody.getElementsByTagName('tr');

        for (let i = 0; i < rows.length; i++) {
            const productName = rows[i].getElementsByTagName('td')[1].textContent.toLowerCase();
            if (productName.indexOf(filter) > -1) {
                rows[i].style.display = '';
            } else {
                rows[i].style.display = 'none';
            }
        }
    }


        function softDeleteProduct(productId,button){
            console.log(productId);
            fetch(`/admin/softDeleteProduct/${encodeURIComponent(productId)}`,{
                method:"POST",
                headers:{
                    "Content-Type":"application/json"
                }
            }).then(res=>res.json())
              .then(data=>{
                if (data.success) {
                button.textContent = data.deleted ? "Unhide" : "Hide";
                swal.fire({
                    // icon:'success',
                    title: `${data.message}`,
                    timer:1000,
                    timerProgressBar:true,
                    showConfirmButton:false,
                    didOpen: () => {
                        const progressBar = Swal.getHtmlContainer().querySelector('.swal2-timer-progress-bar');
                        progressBar.style.backgroundColor = '#dc3545'; 
                    }

                })
                
                } else {

                    swal.fire({
                    // icon:'success',
                    title: 'Failed to update product status',
                    timer:1000,
                    timerProgressBar:true,
                    showConfirmButton:false,
                    didOpen: () => {
                        const progressBar = Swal.getHtmlContainer().querySelector('.swal2-timer-progress-bar');
                        progressBar.style.backgroundColor = '#dc3545'; 
                    }

                })
            
            }
            })
            .catch(error => {
            console.error('Error:', error);
            swal.fire({
                    // icon:'success',
                    title: `${error.message}`,
                    timer:1000,
                    timerProgressBar:true,
                    showConfirmButton:false,
                    didOpen: () => {
                        const progressBar = Swal.getHtmlContainer().querySelector('.swal2-timer-progress-bar');
                        progressBar.style.backgroundColor = '#dc3545'; 
                    }

                })
        });
        }
    </script>


    
  
</body>
</html>
